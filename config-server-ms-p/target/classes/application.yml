server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ddfdesign2018/SpringConfig
          # uri: https://github.com/diegodf8/SptringConfig
